;;; private/gptel/config.el -*- lexical-binding: t; -*-

(use-package! gptel
  ;:init
  ;(load-library "~/.config/doom/private/gptel/apikeys.el.gpg") ;didn't work moved to main config?
  :config
  ;(setq! gptel-api-key "fakekey")
  ;(setq! gptel-api-key (alist-get 'openai llm-apikey-alist))
  )
